# Database configurations are stored in database-config.yml
# Both serverless.yml and DML scripts read from this central file
# No environment variables needed
# 
# To add a new environment:
# 1. Add stage configuration to database-config.yml
# 2. Run: npm run update-db:your-stage