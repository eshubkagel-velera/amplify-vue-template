{
  "version": "2018-05-29",
  "statements": [
    #set($updates = [])
    #foreach($entry in $ctx.args.input.entrySet())
      #if($entry.key != "REDIRECT_URL_ID" && $entry.value)
        $util.qr($updates.add("$entry.key = '$entry.value'"))
      #end
    #end
    "UPDATE REDIRECT_URL SET $util.str.join($updates, ', ') WHERE REDIRECT_URL_ID = '$ctx.args.input.REDIRECT_URL_ID'"
  ]
}